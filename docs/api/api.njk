---
pagination: 
    data: apis
    size: 1
    alias: api
permalink: "api/{{ api.name | slug }}/"
layout: api.njk
---

<h1>{{api.name}}</h1>
{%- for child in api.description.children %}

{% if child.type == 'paragraph' %}
<p>
    {%- for content in child.children %}
        {% if content.type == 'text' %}
            {{ content.value }}
        {% endif %}
        {% if content.type == 'html' %}
            {{ content.value | safe }}
        {% endif %}
        {% if content.type == 'inlineCode' %}
            <code>{{ content.value }}</code>
        {% endif %}
        {% if content.type == 'link' %}
                    {% if 'http' in content.url %}
                        <a href="{{ content.url }}">{{ content.url }}</a>
                    {% else %}
                        <a href="../{{ content.url }}">{{ content.url }}</a>

                    {% endif %}
        {% endif %}
    {% endfor -%}
</p>
{% endif %}
{% endfor -%}

<h3>Examples</h3>
{%- for example in api.examples %}
<pre><code>{{ example.description }}</code></pre>
{% endfor -%}

<h3>Parameters</h3>
<ul style="columns: auto">
{%- for parameter in api.params %}
<li>
<code>{{ parameter.name }}</code>
<td><code>{{ parameter.type.name}}</code></h4> (default <code>{{ parameter.default}}</code>)
<code>{{ parameter.default}}</code>
</li>
{% endfor -%}
</ul>